replicaCount: 3

env:
  OIDC_ISSUER_URI: "https://login.microsoftonline.com/your-tenant-id/v2.0"
  OIDC_AUDIENCE: "api://project-hub"

ingress:
  enabled: true
  hosts:
    - host: project-hub.example.com
      paths:
        - path: /
          pathType: Prefix
